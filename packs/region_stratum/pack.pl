% State the fact: name(region_stratum) — the pack for the brain-region stratum (ordinal 9): the selector.
name(region_stratum).
% State the fact: version('0.1.0') — a safety-layer prototype.
version('0.1.0').
% State the fact: title naming the region stratum and the basal-ganglia action selector at it.
title('Connectome arbiter — region_stratum (ordinal 9): the basal-ganglia action selector and its membership guard').
% State the fact: author is the PrologAI Community.
author('PrologAI Community', 'ai.university.aiu@gmail.com').
% State the fact: home points at the connectome-arbiter repository.
home('https://github.com/ai-university-aiu/connectome-arbiter').
% State the fact: download points at the repository releases page.
download('https://github.com/ai-university-aiu/connectome-arbiter/releases').
% State the fact: requires([]) — dependencies are declared as library imports in the module.
requires([]).
% State the fact: layer(2) — the selector's region stratum sits above the synaptic gain it reads.
layer(2).
% State the fact: stratum(region) — the N6 binding declaration; its ordinal (9) is read from the structure records.
stratum(region).
